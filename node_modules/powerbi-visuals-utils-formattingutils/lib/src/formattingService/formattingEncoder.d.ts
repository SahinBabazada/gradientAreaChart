export declare function removeLiterals(format: string): string;
export declare function preserveLiterals(format: string, literals: string[]): string;
export declare function restoreLiterals(format: string, literals: string[], quoted?: boolean): string;
